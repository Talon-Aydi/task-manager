To run the project, run
```composer install``
```npm install```

After installing all the dependencies, create the .env
```cp .env.example .env```

To proceed and run the web application, generate a key by executing the following line 
```php artisan key:generate```

Once the .env docum